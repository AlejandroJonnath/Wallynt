/**
 * features/auth — Punto de entrada público de la feature de autenticación.
 * Solo exportar lo que otras features o la capa de app necesiten consumir.
 */
export { useAuthStore } from './store/useAuthStore';
