/**
 * core/services — Servicios base reutilizables (no pertenecen a una feature específica).
 * Por ejemplo: servicio de notificaciones, storage, analytics SDK, etc.
 */

// Placeholder — agregar servicios base aquí a medida que se necesiten.
export {};
