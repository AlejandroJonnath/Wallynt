/**
 * core/auth — Lógica de autenticación a nivel de infraestructura.
 * El store de auth vive en @features/auth/store/useAuthStore.
 * Este módulo es para helpers de sesión de bajo nivel si se necesitan.
 */

// Placeholder — agregar helpers de auth aquí si se necesitan.
export {};
