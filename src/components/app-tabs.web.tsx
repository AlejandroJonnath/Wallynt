/**
 * @deprecated — Usar @shared/components/AppTabs en su lugar.
 * Este archivo existe como capa de compatibilidad durante la migración.
 */
export { default, TabButton, CustomTabList } from '@shared/components/AppTabs/AppTabs.web';
