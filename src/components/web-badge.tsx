/**
 * @deprecated — Usar @shared/components/WebBadge en su lugar.
 * Este archivo existe como capa de compatibilidad durante la migración.
 */
export { WebBadge } from '@shared/components/WebBadge';
