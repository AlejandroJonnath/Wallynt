/**
 * @deprecated — Usar @features/auth/store/useAuthStore en su lugar.
 * Este archivo existe como capa de compatibilidad durante la migración.
 */
export { useAuthStore } from '@features/auth/store/useAuthStore';
