/**
 * @deprecated — Usar @shared/components/Toast en su lugar.
 * Este archivo existe como capa de compatibilidad durante la migración.
 */
export { useToast, ToastProvider } from '@shared/components/Toast';
export type { ToastType } from '@shared/components/Toast';
